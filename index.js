/**
 * @format
 */

import {AppRegistry} from 'react-native';
import FatherApp from './FatherApp';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => FatherApp);
